function sig =treno(frame_length,T) 
sig = zeros(1, frame_length);
for f = 1: frame_length
    if (f./frame_length = floor(f./frame_lengt))
        sig(f) = 1;
    else
        sig(f) = 0;
    end
end
end

